#include <stdio.h>
int main(void)
{
    int hundred = 100; // chaque fois tu doit ecrire le type de varibale

    printf("Salut bro ca fait %d ans!\n", hundred); // %machin sapelle format, qui permets de prendre le valeur de variable
    // \n => new line 
    return 0;
}